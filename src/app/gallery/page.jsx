import GalleryClient from './GalleryClient';

export const metadata = {
  title: 'Gallery — GCI Global Computer Institute',
  description: 'Explore our campuses, computer labs, classrooms, student activities, and summer camp programs at GCI Global Computer Institute, Karachi.',
};

export default function GalleryPage() {
  return <GalleryClient />;
}
